#include <iostream>

int main()
{
    int n;
    int count = 0;
    std::cin >> n;
    while (n--) {
        int a, b, c;
        std::cin >> a >> b >> c;
        if (a + b + c >= 2) count++;
        
    }
    std::cout << count;


}
