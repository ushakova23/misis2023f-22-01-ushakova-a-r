#include <iostream>
#include <cmath>

int main() {
    int n;
    std::cin >> n;

    if (n == 1 || (n > 1 && (n & (n - 1)) == 0)) {
        std::cout << "YES" << std::endl;
    } else {
        std::cout << "NO" << std::endl;
    }

    return 0;
}
