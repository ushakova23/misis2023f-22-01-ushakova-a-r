# improc

## Build & Run
### Windows
Run in cmd:
```bash
.build.bat
```

## Add lab

1. create lab(№) directory
2. add `add_subdirectory(lab(№))` to /prj.lab/CMakeLists.txt
3. run `.build.bat` in cmd
